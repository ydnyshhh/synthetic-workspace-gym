# Deterministic counterfactual example

Generate this example with the quick-start commands in `docs/counterfactual-branching.md`. The checked-in records illustrate the versioned output shapes without bundling evaluator secrets.
